#pragma once
#include <iostream>
#include <time.h>
#include <math.h>
#include <algorithm>

using namespace std;

void Zadacha1_1();

void CreatPolid(char* str, int length);
bool isPolind(char* str, int length);
int Strlength(char* str);


void Zadacha1_2();
bool isRepeat(int, int, int, int*);
void Logic(int sizeArr, int* arr, int** arrRes, int& sizearrRes);